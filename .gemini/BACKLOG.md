# 💡 BACKLOG
> Future Ideas. The Icebox.

## [New] DORA Metrics Dashboard Output
- **Problem:** Currently `scripts/generate_metrics.py` outputs a basic JSON file. There is no visual representation for the user.
- **Solution:** Update the `/vector:feedback` or create a new `/vector:metrics` command that reads `METRICS.json` and prints a markdown table/dashboard of the success rates and durations.
- **Impact:** Increases visibility into workflow efficiency.
