# SPEC: Eliminate Redundancy in GEMINI.md

## Objective
Resolve the duplicate content between `AGENTS.md` and top-level `GEMINI.md`.

## Requirements
- Review `GEMINI.md` and `AGENTS.md` for identical concepts.
- Strip redundant procedural rules from `GEMINI.md`.
- Convert `GEMINI.md` into a concise entry point/manifest that points to `AGENTS.md` for detailed rules.
