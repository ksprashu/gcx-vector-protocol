# SKILL_LOG

- Replaced `name: vector-protocol` with `name: vector` in `skills/vector/SKILL.md`.
- Updated `description` to explicitly mention its role as the procedural engine for the commands (`/vector:plan`, `/vector:work`).
