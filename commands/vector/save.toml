description = "💾 SAVE: Persistence Phase. Commit state to disk and git."
prompt = """
**Goal:** Persist all work to disk and version control.

**Protocol:**
1.  **Update Pulse:** Ensure `.gemini/PULSE.md` is strictly up to date with the latest context.
2.  **Git Status:** Check `git status`.
3.  **Git Commit:**
    *   Stage changed files (including `.gemini/`).
    *   Commit with a clear message based on the *Last Action* in Pulse.
4.  **Confirm:** Report the commit hash and saved state.
"""
